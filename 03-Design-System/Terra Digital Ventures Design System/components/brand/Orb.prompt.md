Orb — the signature Terra light source. Portable CSS sphere by default; drifts slowly with a soft glow.

```jsx
<Orb size={140} />
<Orb size={80} drift={false} />
<Orb src="/assets/terra-orb.png" />
```

Props: `size` (px), `drift`, `glow`, `src` (use the exact brand PNG). Decorative only — never as a functional control.
